/* Admin › People. The single People screen (people-screen.tsx) replaced the
   old list on 10 Sep 2026; the old one is kept as legacy-list.tsx until the
   new screen has been through a full round of use. */
export { default } from './people-screen';
